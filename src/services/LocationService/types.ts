export interface ICountry {
  id: number | null;
  name: string;
  iso2: string;
}

export interface IState {
  id: number | null;
  name: string;
  iso2: string;
}

export interface ICity {
  id: number | null;
  name: string;
}
